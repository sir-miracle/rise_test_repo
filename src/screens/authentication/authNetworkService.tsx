import { useContext, useState } from 'react'
import { useMutation } from '@tanstack/react-query'
import { login, signUp } from "../../network_services/networks";

const useSignUpService = () => {

}

const useSignInService = () => {

}